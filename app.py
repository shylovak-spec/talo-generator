import streamlit as st
import pandas as pd
from database import EQUIPMENT_BASE
import datetime

st.set_page_config(page_title="Talo КП Generator", page_icon="⚡", layout="wide")

st.title("⚡ Генератор комерційних пропозицій ТОВ «Тало»")

# --- БЛОК 1: ШАПКА КП ---
with st.expander("📌 Основна інформація про замовлення", expanded=True):
    col1, col2, col3 = st.columns(3)
    with col1:
        customer = st.text_input("Замовник", "ОСББ Вишгородська 45")
        address = st.text_input("Адреса об'єкта", "м. Київ, вул. Вишгородська 45, к. 9")
    with col2:
        kp_num = st.text_input("Номер КП", "1223.25POW-B")
        manager = st.text_input("Відповідальний", "Олексій Крамаренко")
    with col3:
        date = st.date_input("Дата", datetime.date.today())
        phone = st.text_input("Телефон", "+380 (67) 477-17-18")

# --- БЛОК 2: ТЕХНІЧНЕ ЗАВДАННЯ (Червона текстовка з файлу) ---
st.subheader("📝 Детальні технічні умови")
st.info("Відредагуйте пункти нижче відповідно до потреб замовника (червона частина в шаблоні)")

col_left, col_right = st.columns(2)

with col_left:
    txt_intro = st.text_area("Вступна фраза", "Відповідно до наданих даних з метою автономного забезпечення роботи ліфта, насосної ХВП, ІТП та освітлення ліфтових холів та фоє пропонуємо наступний комплекс обладнання та робіт.")
    line1 = st.text_input("Пункт 1 (Ліфти)", "Організація автономного живлення ліфтів в/п 1000 та 630 кг до 8 годин автономної роботи, 2 години від мережі загального користування з повним зарядом батарей;")

with col_right:
    line2 = st.text_input("Пункт 2 (Насоси/ІТП)", "Організація автономного живлення насосної та ІТП від 6-8 годин автономної роботи, 4 години від мережі загального користування з повним зарядом батарей;")
    line3 = st.text_input("Пункт 3 (Освітлення/Безпека)", "Електрозабезпечення аварійного освітлення, домофона та відеонагляду;")

# --- БЛОК 3: ВИБІР ОБЛАДНАННЯ ---
st.subheader("📦 Специфікація обладнання та робіт")

all_selected_data = []
categories = list(EQUIPMENT_BASE.keys())
tabs = st.tabs(categories)

for i, cat in enumerate(categories):
    with tabs[i]:
        available_items = EQUIPMENT_BASE[cat]
        selected_for_cat = st.multiselect(f"Оберіть позиції ({cat}):", list(available_items.keys()), key=cat)
        
        for item in selected_for_cat:
            c_name, c_qty, c_price, c_total = st.columns([4, 1, 2, 2])
            with c_name:
                st.write(f"**{item}**")
            with c_qty:
                qty = st.number_input(f"К-сть", min_value=1, value=1, key=f"qty_{item}")
            with c_price:
                price = available_items[item]
                st.write(f"{price:,} грн")
            with c_total:
                subtotal = price * qty
                st.write(f"**{subtotal:,} грн**")
                all_selected_data.append({"Найменування": item, "Кількість": qty, "Ціна, грн": price, "Сума, грн": subtotal})

# --- БЛОК 4: ПІДСУМКИ ---
if all_selected_data:
    st.write("---")
    df = pd.DataFrame(all_selected_data)
    total_all = df["Сума, грн"].sum()
    
    st.header(f"Загальна вартість: {total_all:,} грн".replace(',', ' '))
    st.table(df[["Найменування", "Кількість", "Ціна, грн", "Сума, грн"]])

    if st.button("🚀 Сформувати файл Word"):
        st.balloons()
        st.success("Дані готові до експорту в шаблон!")
